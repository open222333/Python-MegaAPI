version: "3.9"

services:
  mega_api:
    build:
      context: .                # 指向專案根目錄
      dockerfile: docker/Dockerfile  # 指定 Dockerfile 的路徑
    container_name: mega_api
    hostname: mega_api
    restart: unless-stopped
    environment:
      - TZ=Asia/Taipei
      - LOG_LEVEL=DEBUG
    volumes:
      - ./conf:/app/conf        # 保留設定檔
      - ./logs:/app/logs        # 保留日誌
    command: ["python", "main.py"]
